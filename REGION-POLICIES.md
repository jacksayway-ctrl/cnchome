# 접수 지역 정책

`korea-regions.js`는 업로드 이미지와 독립적인 전국 시·군·구 기준 목록입니다. 공식 출처와 확인일, 상위 시·군, 동명 지역을 구별하는 시도 키를 포함합니다. 기존 정책의 시도 표기는 업무상 구분으로 유지하며 새 행정구역으로 임의 병합하지 않습니다.

`region-rules.js`는 원문을 바꾸지 않고 적용 범위와 제외 조건을 분리합니다.

| 정책 | 처리 |
| --- | --- |
| 충남 북부: 천안시, 아산시 必 | 명시한 두 시만 허용. 북부에 속하는 다른 시를 추정하지 않음 |
| 경남 전체 (창원, 김해, 양산 제외) | 해당 행의 공유 수량에서 세 시를 제외. 별도 행에 명시된 수량은 적용 가능 |
| 서산시 전체 (대산읍 제외) | 서산시는 일부 제한. 대산읍은 해당 행에서 제외 |
| 서산시: 대산읍, 지곡면 | 두 하위 지역만 허용. 시 전체로 확대하지 않음 |
| 별도 접수 불가 지역 열·행 | 해당 지역을 우선 차단. 다른 접수 코드·상품에는 전파하지 않음 |
| 수량 0 | 해당 정책 범위 접수 불가 |

수량은 원래 행의 지역들이 공유합니다. 시·군별 합계로 늘리거나 여러 행의 수량을 임의로 합산하지 않습니다. 충돌하는 수량은 확인 대상으로 표시합니다. 자동 오타 치환은 하지 않습니다.

관리자의 등록 접수 코드·상품 선택 후 등록하면 직원 화면의 목록과 지도에 동일한 규칙을 사용합니다. 전체 가능·일부 제한·불가·확인 필요를 구분합니다. 읍·면·동 경계 자료는 없으므로 상세 목록을 표시하고, 면 일부 제외를 시·군 전체 불가로 칠하지 않습니다. 기존 지도와 달라진 구 경계는 추정하지 않습니다.

현재 GitHub Pages 앱의 저장은 `localStorage`를 사용합니다. 새로고침과 같은 브라우저의 다른 탭에서 유지됩니다. 별도 사용자·기기 사이에 공유하는 서버 저장소나 실제 접수 시 수량 차감은 연결되지 않았습니다.

## 접수 코드 관리

한화·신한·G/A는 지역명이 아닌 기본 접수 코드입니다. `intake-codes.js`가 확장 가능한 목록을 관리합니다. 관리자는 접수 관리 또는 운영 설정에서 새 코드와 표의 다른 표기를 추가하고 이름을 수정할 수 있습니다. 중복된 이름·표기는 저장하지 않습니다. 이름을 수정해도 내부 식별자가 유지되어 기존 지역·수량 정책이 연결되며 이전 이름도 표제로 인식합니다.

단독 접수 코드 표제와 별도 접수 코드 열을 지역명 검사에서 분리합니다. 완전히 일치하는 등록 표기만 인식하며 OCR 원문은 치환하지 않습니다. 여러 코드가 섞인 표, 미등록 코드가 명시된 표, 선택한 코드와 표의 코드가 다른 경우는 등록을 막아 정책이 섞이지 않도록 합니다. 새 코드의 지역 정책을 등록하면 직원 목록과 지도에도 표시됩니다. 접수 입력 체험의 코드 목록도 같은 자료를 사용합니다.

코드 목록은 `cnchome.intakeCodes.v1`, 지역 정책은 기존 `cnchome.regionPolicies.v1`에 저장합니다. 저장 실패 시 기존 목록을 유지합니다. 새로고침과 같은 브라우저의 다른 탭에 변경을 반영하며 별도 기기 간 공유·관리자 인증 서버는 연결되지 않은 미리보기입니다.

## 검증

```sh
node --test intake-codes.test.cjs intake-code.integration.test.cjs
node --test region-rules.test.cjs
node --test korea-regions.test.cjs
node --test ocr-review.test.cjs
node --test ocr-row-verification.test.cjs
```

OCR 경고가 있는 행만 모아 획 보존 확대 영상으로 다시 판독합니다. 필요한 경우에만 색상 채널 영상을 한 번 더 판독합니다. 서로 다른 영상의 실제 판독이 신뢰도 기준을 충족하고 일치해야 경고를 해제합니다. 한자 행은 혼합 모델의 반복 판독과 한국어 모델의 한글 부분을 따로 확인합니다. 정상 행은 추가 OCR 대상에서 제외하고, 사전 후보로 글자를 바꾸지 않습니다. 재판독이 실패하면 기존 판독과 확인 경고를 유지합니다.
